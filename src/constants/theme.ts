// Design tokens (colors, fonts, spacing) should be defined here following the Stitch Design Protocol.

export const colors = {
  // fill with hex values from design
};

export const fonts = {
  // font families and sizes
};
